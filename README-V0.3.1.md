# V0.3.1 — Historique des opérations

Cette version conserve les workflows actuels synchrones et stables.

Ajouts :
- stockage persistant dans `/data/jobs.json`;
- enregistrement des résultats de déploiement;
- enregistrement des résultats de claim Plex;
- historique affiché dans la fiche AppBox;
- endpoint de lecture `/api/appboxes/{client_id}/jobs`;
- Docker CLI 29 et plugin Compose conservés.

Aucune action n'est exécutée en arrière-plan dans cette version.
